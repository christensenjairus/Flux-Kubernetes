apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - repository-vm.yaml
  #- repository-grafana.yaml
  #- repository-prometheus.yaml
  - release.yaml

configMapGenerator:
  - name: victoria-metrics-values
    files:
      - values.yaml=values.yaml

configurations:
  - kustomizeconfig.yaml