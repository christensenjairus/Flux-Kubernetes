apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: robusta
resources:
  - release.yaml
  - repository.yaml

configMapGenerator:
  - name: robusta-values
    files:
      - values.yaml=values.yaml

configurations:
  - kustomizeconfig.yaml