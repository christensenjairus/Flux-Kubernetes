apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: snapshot-controller-crds
  namespace: rook-ceph
spec:
  install:
    crds: CreateReplace
  upgrade:
    crds: CreateReplace
  interval: 30m
  chart:
    spec:
      chart: snapshot-controller-crds
      version: "*"
      sourceRef:
        kind: HelmRepository
        name: wiremind
        namespace: rook-ceph
      interval: 12h