---
apiVersion: "cilium.io/v2alpha1"
kind: CiliumLoadBalancerIPPool
metadata:
  name: cilium-loadbalancer-ip-pool
  namespace: kube-system
spec:
  blocks:
    - start: "${ipv4LBPrefix:=10.0.0}.200"
      stop: "${ipv4LBPrefix:=10.0.0}.254"
