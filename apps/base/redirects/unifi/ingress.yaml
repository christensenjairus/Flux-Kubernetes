apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    nginx.ingress.kubernetes.io/secure-backends: "true"
  name: unifi
  namespace: redirects
spec:
  ingressClassName: nginx-private
  rules:
    - host: unifi.christensencloud.us
      http:
        paths:
          - backend:
              service:
                name: unifi
                port:
                  number: 443
            path: /
            pathType: Prefix
  tls:
    - hosts:
        - unifi.christensencloud.us
      secretName: unifi-christensencloud.us-tls
