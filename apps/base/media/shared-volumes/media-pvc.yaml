apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media
  namespace: media
spec:
  accessModes:
    - ReadWriteMany
    - ReadWriteMany
    - ReadOnlyMany
  resources:
    requests:
      storage: 75000Gi
  volumeMode: Filesystem
  volumeName: media
  storageClassName: ''
