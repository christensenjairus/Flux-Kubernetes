apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: podinfo
  namespace: podinfo
spec:
  driftDetection:
    mode: enabled
  chart:
    spec:
      version: ">=1.0.0"
  values:
    ingress:
      hosts:
        - host: podinfo-epsilon.christensencloud.us
          paths:
            - path: /
              pathType: ImplementationSpecific
