apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: my-new-pvc
spec:

# ------------ Edit below --------------------
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 3Gi
  storageClassName: ceph-filesystem-3
  dataSource:
    kind: PersistentVolumeClaim
    name: nextcloud-nextcloud-new