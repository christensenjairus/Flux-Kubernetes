apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: my-new-pvc
spec:

# ------------ Edit below --------------------
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
  storageClassName: ceph-block-3